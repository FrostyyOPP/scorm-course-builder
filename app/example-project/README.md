# Example project

A template showing the folder layout. Drop your own files into `videos/`, `readings/`,
and `quizzes/`, edit `outline/outline.md`, then run `node src/index.js example-project`.

Media files (.mp4/.docx) are gitignored — add your own.
